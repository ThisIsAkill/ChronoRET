services:
  ct-docs:
    build: .
    container_name: ct-docs
    restart: unless-stopped
    # Expose on a port your reverse proxy can pick up.
    # If you use Traefik/Caddy/nginx-proxy, remove the ports block
    # and add whatever labels your stack needs instead.
    ports:
      - "8765:80"
